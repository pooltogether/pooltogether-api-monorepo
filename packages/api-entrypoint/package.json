{
  "private": true,
  "name": "@pooltogether/api-entrypoint",
  "version": "1.0.0",
  "description": "A cloudflare worker to read data from the KV",
  "main": "index.js",
  "scripts": {
    "start": "wrangler dev",
    "dev": "wrangler dev",
    "start-prod": "wrangler dev --env production",
    "dev-prod": "wrangler dev --env production",
    "publish-prod": "wrangler publish --env production",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "reinstall": "rm -rf node_modules && yarn install"
  },
  "author": "Dylan DesRosier <dylan@pooltogether.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "@pooltogether/current-pool-data": "^3.4.25",
    "ethers": "^5.4.1"
  }
}
