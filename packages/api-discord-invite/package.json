{
  "name": "@pooltogether/api-gas-updater",
  "version": "1.0.0",
  "description": "A worker to keep the KV fresh with latest gas estimations",
  "main": "index.js",
  "scripts": {
    "build": "wrangler build",
    "dev": "wrangler dev --env main",
    "start": "wrangler dev --env main",
    "publish-main": "rm -rf dist && rm -rf worker && wrangler publish --env main",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "PoolTogether <hello@pooltogether.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "@glenstack/cf-workers-hcaptcha": "^1.0.0",
    "hcaptcha": "^0.1.0"
  }
}
